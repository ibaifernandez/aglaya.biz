# Build output
dist/
.astro/

# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# Netlify
.netlify/

# macOS
.DS_Store

# Logs
npm-debug.log*
*.log

# Editor
.vscode/
.idea/

# Claude
.claude

# Playwright test artifacts
test-results/
playwright-report/

# Misc
/docs/aglaya-full.png
/docs/aglaya-full-2.png

# AI tool configs
.notebooklm/
graphify-out/

# Delivery artifacts (client-facing exports — tracked externally)
docs/delivery/

# Scanner infographics belong to auditoria-ley21719.cl, not here
src/assets/images/infografia-ley-21719-*.png

# Transient mutant written by tests/unit/consent-seal-guard.test.ts (deleted in finally)
tests/unit/__seal-mutant.gen.test.ts

# Tailwind bridge, rebuilt from @aglaya/design-tokens on every install/build.
# Committing it would re-create the copy this repo just stopped making: it would
# pass every check except the mutation proof (scripts/mutation-proof.sh).
src/styles/tokens.theme.generated.css

# Stray Playwright run log — output, not source (was tracked until 2026-08-05)
tests/e2e/results.txt

# Coverage output (reported in CI as an artifact, never committed)
coverage/
